# Dockerfile для разработки с hot-reload
FROM node:22-alpine AS base

WORKDIR /app

# Копируем package.json и package-lock.json
COPY package.json package-lock.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем конфигурационные файлы
COPY next.config.mjs tsconfig.json postcss.config.mjs ./
# tailwind.config может быть встроен в другой файл или отсутствовать - это нормально

# В dev режиме не копируем src и public - они будут смонтированы как volumes
# Это позволяет hot-reload работать без пересборки образа

EXPOSE 3001

# Команда по умолчанию (можно переопределить в docker-compose)
CMD ["npm", "run", "dev:docker"]

